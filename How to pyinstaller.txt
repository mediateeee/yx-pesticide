在项目根目录执行

单文件、无控制台：
pyinstaller -F -w --version-file version.txt yx-pesticide.py --noconfirm

目录形式、无控制台：
pyinstaller -D -w --version-file version.txt yx-pesticide.py --noconfirm

建议使用 7z SFX Builder 将目录打包成单文件。
打包前请确认资源管理器中的文件版本与 yx-pesticide.py 里的 VERSION 一致。
